services:
  - type: web
    name: escola-high-tech-front
    runtime: static
    buildCommand: npm run generate
    staticPublishPath: .output/public
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
